import { type ColumnDef } from '@tanstack/react-table'
import { type AgingReportRow } from '../data/schema'

// Legacy placeholder: not used by the current UI, but kept to satisfy project-wide TS includes.
export const agingReportColumns: ColumnDef<AgingReportRow>[] = []

